# Vale configuration for Hugo blog
# Vale is much smarter than cspell and context-aware

StylesPath = vale-styles

# Only check content files
[*.md]
BasedOnStyles = Vale, Microsoft

# Ignore front matter
MinAlertLevel = suggestion

# Custom rules for blog content
[*.md]
# Allow technical terms
# Allow proper nouns (names, companies)
# Allow code snippets
# Allow URLs
